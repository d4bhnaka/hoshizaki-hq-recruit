---
interface Props {
  en: string;
  ja?: string;
  variant?: "light" | "blue" | "cloud";
  compact?: boolean;
  align?: "left" | "center";
  bg?: string;
  jaSize?: "md" | "lg";
}

const {
  en,
  ja,
  variant = "light",
  compact = false,
  align = "left",
  bg,
  jaSize = "md",
} = Astro.props;

// 背景画像は直接 background-image で指定する（HTMLドキュメント基準で相対解決され、
// サブディレクトリ配置でもパスが繋がる）。var() 経由だと外部 style.css 基準で解決され、
// サブディレクトリでパスがずれるため避ける。
const style = bg ? `background-image: url('${bg}');` : undefined;
---

<section
  class={`c-page-hero c-page-hero--${variant} c-page-hero--${align}${compact ? " c-page-hero--compact" : ""}${bg ? " c-page-hero--has-bg" : ""}${jaSize === "lg" ? " c-page-hero--ja-lg" : ""}`}
  style={style}
>
  <div class="c-page-hero__inner">
    <h1 class="c-page-hero__title">
      <span class="c-page-hero__en">{en}</span>
      {ja && <span class="c-page-hero__ja">{ja}</span>}
    </h1>
    <slot />
  </div>
  <slot name="decoration" />
</section>
