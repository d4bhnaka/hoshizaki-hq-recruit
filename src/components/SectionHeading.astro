---
interface Props {
  en: string;
  ja?: string;
  note?: string;
  id?: string;
  align?: "left" | "center";
  size?: "md" | "lg";
}

const { en, ja, note, id, align = "left", size = "md" } = Astro.props;
---

<header class={`c-section-heading c-section-heading--${align} c-section-heading--${size}`} id={id}>
  {ja && <p class="c-section-heading__ja">
    <span class="c-section-heading__icon" aria-hidden="true"></span>
    {ja}
  </p>}
  <h2 class="c-section-heading__en">{en}</h2>
  {note && <p class="c-section-heading__note">{note}</p>}
</header>
